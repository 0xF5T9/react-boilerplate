/**
 * @file jsdb-endpoints.js
 * @description json-server database endpoints.
 */

'use strict';

const jsdbEndpoints = {
    posts: 'posts',
    comments: 'comments',
    profile: 'profile',
};

export default jsdbEndpoints;
